# Device-specific OTA signing keys

These are the OTA signing keys used in the Blecon firmware loaded in the factory.
When deploying firmware updates to Blecon devices running stock Blecon firmware,
you must use the key corresponding to a device type.
